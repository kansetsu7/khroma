<div class = "container">
  <div class="jumbotron jumbotron-fluid">
    <div class="container">
      <ul class="nav nav-pills nav-justified">     
        <% @genders.each do |gender| %>
          <li class="nav-item">
            <%= link_to gender.name, gender_categories_path(gender.id), class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>



 
  <%= render partial: "shared/recommend", locals: {objs: @genders} %>
</div>